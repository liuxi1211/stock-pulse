# 自选股模块功能补齐 - 验证检查清单

## 表结构与数据层
- [ ] `sys_watchlist` 表包含 group_id / note / target_price_high / target_price_low / sort_order 5 个新字段，均允许 NULL
- [ ] `idx_sys_watchlist_group (user_id, group_id)` 索引已创建
- [ ] `sys_watchlist_group` 表已创建，字段完整（id / user_id / group_name / created_at / sort_order）
- [ ] `UNIQUE(user_id, group_name)` 唯一约束已添加
- [ ] MySQL / SQLite / 注释 三份 schema 同步更新
- [ ] `WatchlistItemDO` 包含 5 个新属性，命名与映射正确
- [ ] `WatchlistGroupDO` + `WatchlistGroupMapper` 编译通过
- [ ] 已有自选股数据在表结构扩展后不报错（向后兼容）

## Service 层
- [ ] `WatchlistService` 接口扩展了分组/提醒/排序/批量操作等方法
- [ ] `WatchlistGroupService` 接口 + 实现类完整
- [ ] `getWatchlist` 支持 groupId 过滤 + sortBy 排序，返回 VO 含所有扩展字段
- [ ] 批量操作（batchRemove / batchMoveGroup）加了 `@Transactional` 事务注解
- [ ] 删除分组时组内股票 group_id 置 NULL，股票本身不删除
- [ ] JOIN 查询 SQL 走索引，无 N+1 问题
- [ ] Service 层异常处理规范，错误码正确

## Controller 层
- [ ] `WatchlistController` 扩展了 7 个新接口（提醒/排序/分组/批量）
- [ ] `WatchlistGroupController` 4 个 CRUD 接口完整
- [ ] 所有接口返回 `ApiResponse<T>` 统一格式
- [ ] 参数 >5 个的接口封装为 DTO 对象
- [ ] GET /watchlist 的 query 参数合计 ≤ 5 个
- [ ] Swagger 注解完整，接口描述清晰
- [ ] 请求/返回体无 `Map<?,?>` 类型

## 前端脚本独立化
- [ ] `static/js/watchlist.js` 文件存在
- [ ] IIFE 封装，暴露 `WatchlistPage` 全局对象，含 `init()` 方法
- [ ] `watchlist.html` 删除内联业务脚本，用 `<script th:src="@{/js/watchlist.js}">` 引入
- [ ] common.js / search-suggest.js / echarts 加载顺序正确
- [ ] 二次访问时 watchlist.js 走浏览器缓存
- [ ] 浏览器控制台无 `is not defined` 报错

## 分组管理
- [ ] 左侧分组栏渲染正常，含「全部」+ 行业分组 + 自定义分组 + 「未分组」
- [ ] 行业分组按申万一级行业自动归类，与 sw_industry_member 表数据一致
- [ ] 新建自定义分组正常，分组栏实时更新
- [ ] 分组重命名正常
- [ ] 删除分组时二次确认，组内股票移至「未分组」
- [ ] 点击分组切换，中间表格对应刷新
- [ ] sw_industry_member 无数据时，分组栏不显示行业分组（降级正常）

## 价格提醒
- [ ] 列表每行有「设提醒」按钮或小铃铛图标
- [ ] 提醒设置弹窗支持「按目标价」和「按涨跌幅」两种模式
- [ ] 涨跌幅模式保存时正确换算为等价目标价
- [ ] 保存成功后列表行显示小铃铛图标
- [ ] 前端 60 秒轮询最新行情，比对阈值
- [ ] 阈值触达时顶部通知条 + toast + 行高亮三重提醒
- [ ] 通知条显示股票名 + 代码 + 当前价 + 触发条件
- [ ] 点击通知条跳转个股诊断页
- [ ] 同一股票同一阈值 5 分钟内只提醒一次（去重）
- [ ] 清除提醒后小铃铛消失，不再触发
- [ ] 后台 tab 轮询降频为 5 分钟
- [ ] 页面卸载时定时器被清除（无内存泄漏）

## 搜索添加
- [ ] 顶部搜索框存在，placeholder 提示清晰
- [ ] SearchSuggest 集成正常，输入 ≥2 字符触发联想
- [ ] 拼音首字母联想正常（如 zsyh → 招商银行）
- [ ] 联想项显示代码 + 名称 + 所属行业
- [ ] 点击联想项直接添加到自选股，toast 提示成功
- [ ] 重复添加时 toast 提示「已在自选股中」，不重复添加
- [ ] 当前选中自定义分组时，新添加股票自动归入该分组

## 列表排序
- [ ] 顶部排序下拉框存在，含 4 个选项
- [ ] 按涨跌幅降序：涨幅大的在前
- [ ] 按添加时间降序：最新添加的在前
- [ ] 按字母升序：按名称拼音 A-Z
- [ ] 按自定义顺序：按 sort_order 升序，行可拖拽
- [ ] 切换排序方式后列表立即刷新，无明显卡顿

## 批量操作
- [ ] 表格第一列有复选框，表头有全选复选框
- [ ] 全选/反选功能正常
- [ ] 勾选至少 1 行后，顶部显示批量操作按钮
- [ ] 未勾选时批量按钮隐藏或置灰
- [ ] 批量移除二次确认，成功后 toast 提示数量
- [ ] 批量改分组弹窗选择目标分组，确认后生效
- [ ] 批量操作有事务保证，中途失败全部回滚

## mini K 线
- [ ] 「30 日趋势」列表存在，每行显示 sparkline 折线
- [ ] 30 日整体涨：红色折线 + 红色填充
- [ ] 30 日整体跌：绿色折线 + 绿色填充
- [ ] 鼠标悬停显示 tooltip：起止价 / 最高 / 最低 / 涨跌幅
- [ ] 新股不足 30 日时显示已有数据，提示「数据不足 30 日」
- [ ] 翻页/切换分组时，旧 ECharts 实例 dispose（无内存泄漏）
- [ ] 50 条 mini K 线首屏渲染 ≤ 1 秒
- [ ] mini 图尺寸符合设计（约 100px × 30px）

## 列扩展与格式化
- [ ] 列表显示行业 / 总市值 / PE(TTM) / PB / 换手率 5 个新列
- [ ] 总市值：万元转亿元，保留 2 位小数，显示「亿」单位
- [ ] PE / PB：保留 2 位小数，负值或 NULL 显示「-」
- [ ] 换手率：百分比格式，保留 2 位小数
- [ ] 行业：申万一级行业中文名
- [ ] daily_basic 无数据时 5 列显示「-」（降级正常）
- [ ] 数字格式化复用 `StockApp.formatNumber` 等统一方法

## 股票代码跳转
- [ ] 股票代码列显示为链接（主题蓝色，悬停下划线）
- [ ] 点击正确跳转 `/page/stock-detail/{code}`
- [ ] 个股诊断未上线时有降级处理（纯文本或 404 提示）

## 拖拽排序
- [ ] 自定义顺序模式下，表格行可拖拽调整顺序
- [ ] 拖拽后调用接口更新 sort_order，刷新后保持
- [ ] 行可拖拽到左侧分组名上，跨分组移动
- [ ] 拖拽视觉反馈清晰（半透明、放置指示、目标高亮）
- [ ] 非自定义顺序模式下拖拽行为合理（禁用或自动切换）

## 主题兼容
- [ ] azure / mist / cyber 三主题切换后列表颜色正确
- [ ] mini K 线涨跌色随主题变化（使用主题变量）
- [ ] 分组栏样式随主题变化
- [ ] 通知条 / toast 样式与主题协调
- [ ] 无硬编码颜色值，均使用 CSS 变量或主题类

## 整体体验
- [ ] 首屏加载 ≤ 2 秒（含 50 条 mini K 线）
- [ ] 列表顶部显示数据日期（YYYY-MM-DD）
- [ ] 端到端流程：加入 → 分组 → 设提醒 → 触发 → 跳转，全流程顺畅
- [ ] 三主题切换后布局不乱
- [ ] 浏览器控制台无报错
- [ ] 响应式布局在 1366×768 分辨率下正常

## 数据降级
- [ ] daily_basic 无数据：5 列显示「-」，基础行情正常
- [ ] daily_quote 无数据：mini K 线显示「暂无数据」，涨跌幅显示「-」
- [ ] sw_industry_member 无数据：行业列显示「-」，分组栏无行业分组
- [ ] 个股诊断未上线：股票代码降级为纯文本或 404 友好提示
- [ ] 所有降级场景下页面不白屏、不报错
