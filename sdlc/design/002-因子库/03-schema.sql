-- ============================================================
-- 002 因子库 新增表与索引（本次：零新增）
-- 生成时间：2026-06-20
-- 设计依据：PRD sdlc/prd/002-因子库/002-因子库PRD.md
--         + 因子清单 sdlc/prd/002-因子库/20个标准股票因子.json
-- 说明：本模块为纯计算服务，完全复用现有表结构
--       （daily_quote / stock_basic / trade_cal / adj_factor / sys_user），
--       不新增任何表 / 索引 / 列。
--       选股模块的指标持久化在 005 设计中另行处理，
--       其列名规则必须复用本 PRD §9.3（factorKey_paramValue /
--       factorKey_outputLabel / 列名小写）。
-- ============================================================

-- 预留位：本文件不包含任何 CREATE / ALTER 语句。
-- 在执行 schema.sql 迁移流程时，可直接将本文件作为"空变更"的锚点，
-- 便于在后续有扩展字段需求时在此处追加 ALTER 语句。
