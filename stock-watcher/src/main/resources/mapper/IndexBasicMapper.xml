<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arthur.stock.mapper.IndexBasicMapper">

    <insert id="insertBatch">
        INSERT INTO index_basic (ts_code, name, fullname, market, publisher, index_type, category,
                                 base_date, base_point, list_date, weight_rule) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.tsCode}, #{item.name}, #{item.fullname}, #{item.market}, #{item.publisher},
             #{item.indexType}, #{item.category}, #{item.baseDate}, #{item.basePoint},
             #{item.listDate}, #{item.weightRule})
        </foreach>
    </insert>

    <delete id="deleteAll">
        DELETE FROM index_basic
    </delete>

    <update id="truncateTable">
        TRUNCATE TABLE index_basic
    </update>

    <select id="selectAllTsCodes" resultType="java.lang.String">
        SELECT ts_code FROM index_basic
    </select>

    <select id="selectAllBaseDateMap" resultType="java.util.Map">
        SELECT ts_code AS tsCode, base_date AS baseDate FROM index_basic WHERE base_date IS NOT NULL
    </select>

</mapper>
