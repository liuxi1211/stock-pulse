<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arthur.stock.mapper.AdjFactorMapper">

    <insert id="insertBatch">
        INSERT INTO adj_factor (ts_code, trade_date, adj_factor) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.tsCode}, #{item.tradeDate}, #{item.adjFactor})
        </foreach>
    </insert>

    <delete id="deleteBatchByKeys">
        DELETE FROM adj_factor WHERE
        <foreach collection="list" item="item" separator=" OR ">
            (ts_code = #{item.tsCode} AND trade_date = #{item.tradeDate})
        </foreach>
    </delete>

    <!-- 一次性查出所有股票的最新交易日期，用于增量更新预加载 -->
    <select id="selectLatestDatePerStock" resultType="map">
        SELECT ts_code, MAX(trade_date) AS latest_date FROM adj_factor GROUP BY ts_code
    </select>

    <select id="selectLatestTradeDate" resultType="string">
        SELECT MAX(trade_date) FROM adj_factor
    </select>

    <!-- 统计最近 N 天内关键字段为 NULL 或复权因子 <= 0 的异常记录数 -->
    <select id="countNullInvalidRecords" resultType="int">
        SELECT COUNT(*) FROM adj_factor
        WHERE trade_date &gt;= #{startDate}
          AND (ts_code IS NULL OR trade_date IS NULL OR adj_factor IS NULL OR adj_factor &lt;= 0)
    </select>

    <!-- adj_factor 表中 distinct 股票数量 -->
    <select id="countDistinctStocks" resultType="int">
        SELECT COUNT(DISTINCT ts_code) FROM adj_factor
    </select>

    <!-- 批量查询一组股票在指定日期范围内的复权因子记录数 -->
    <select id="countByTsCodesInRange" resultType="map">
        SELECT ts_code, COUNT(*) AS cnt
        FROM adj_factor
        WHERE ts_code IN
        <foreach collection="tsCodes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
          AND trade_date &gt;= #{startDate}
          AND trade_date &lt;= #{endDate}
        GROUP BY ts_code
    </select>

</mapper>
